package dec;

public class Demo{
	public static void main(String[] args) {
		int array[] = {1,2,3,4,5,6,7};
//		getSumber(Sumber.WINTER);
		// 获取枚举中的对象，并且获得枚举方法中的返回值
		Sumber s = Sumber.WINTER;
		System.out.println(s.getSum(array));
	}
	
	
	
}